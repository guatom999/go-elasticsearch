version: "3.8"

services:
  db:
    image: postgres:13
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: randompassword
      POSTGRES_DB: elastricsearch_db
    ports:
      - 5501:5432
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
